[06.07.2026]
- Rate-limiting на /login
- іконки для категорій
- валідація введеної суми та дати на клієнті (до відправки форми)
- кнопка для перемикання на світлу / темну тему
- smoke / unit тести

[07.07.2026]
- додано pre-commit hook (для коміту мають успішно відпрацювати тести)
- додано блок "Останні записи" (топ-5 останніх значень для доходів та витрат)

[28.07.2026]
- видалення записів з блоку "Останні записи" (з підтвердженням і перевіркою,
  що рядок у таблиці не змінився з моменту відкриття сторінки)
- get_recent_entries розділено на _rows_to_entries + get_recent_entries, додано тести
- виправлено вимкнення rate-limit у тестах (limiter.enabled замість config)
- вікно "Статистика" за обраний період (7/30/90 днів або свій діапазон,
  за замовчуванням — останні 30 днів): загальні суми витрат/доходів і різниця,
  окремі щоденні графіки по витратах і доходах, таблична альтернатива графікам,
  топ-5 категорій витрат і доходів

[29.07.2026]
- редагування записів (модальне вікно з попередньо заповненими даними та перевіркою fingerprint)
- суми на сторінці статистики показуються без копійок (компактніше)
- управління категоріями: ⚙️ кнопка для додавання, видалення, редагування категорій
  (модальне вікно з табами для витрат та доходів, категорії зберігаються у categories.json)
- архівування видалених записів до таблиці DELETED з полями deleted_at та income_or_expense
- додано поле updated_at для відстеження дати редагування запису
- форматування суми з комою (515,7 замість 515.7) при записуванні в таблицю
- тести для перевірки форматування суми з комою в submit та edit маршрутах
- розбиття операції на категорії (checkbox "Розбити на категорії" в формі)
- виправлено відображення та редагування розбитих операцій:
  * видалено знак ₴ з "Останніх записів" для компактності
  * виправлено обрізування назви категорії для split expenses
  * виправлено помилку fingerprint при редаганні split (використовуємо реальну категорію першого рядка)
- додано редагування розбивки: можна змінювати категорії та суми в розбитих операціях,
  невідредаговуваний останній рядок автоматично розраховує залишок суми

[30.07.2026]
- виправлено видалення розбитих операцій: рядки шукаються за split_id, а не за
  номером рядка, тому зсув нумерації в таблиці більше не блокує видалення
  повідомленням "Запис уже змінився або був видалений"
- виправлено кнопку "Зберегти" при редагуванні розбивки: приховане поле категорії
  лишалось required, і браузер мовчки не давав відправити форму
- у модалці редагування розбивки поле суми тепер показує загальну суму операції,
  від якої рахується залишок в останньому рядку
- "+ Додати категорію" в модалці редагування справді додає рядок і не скидає
  вже введені суми; видалення рядка теж їх зберігає
- редагування розбивки коректно змінює кількість категорій: зайві рядки
  видаляються, нові дописуються (раніше в таблиці лишались рядки старої розбивки)
- відредаговані суми записуються в тому ж форматі, що й нові (515,7)
- перегляд статистики в доларах США (окремі кнопки ₴ / $ на вкладці "Статистика"),
  курси НБУ автоматично отримуються на дату операції

[31.07.2026]
- у "Статистиці" додано календарні фільтри "Поточний місяць" і "Попередній місяць"
- додано окремий режим "По місяцях" із фільтрами пів року, рік або власний
  діапазон; він показує графік і таблицю доходів, витрат, чистого результату
  та його відсотка від доходів
- додано необов'язкові підкатегорії з емоджі: вони прив'язані до категорій,
  зберігаються в окремій колонці, доступні для нових і попередніх операцій та
  керуються у вікні категорій

[01.08.2026]
- виправлено дату за замовчуванням у формі та статистиці: сервер працює в UTC,
  тепер "сьогодні" рахується за київським часом (today_kyiv()), а не часом
  сервера — раніше вночі (різниця у 2-3 год залежно від DST) дата "з'їжджала"
  на день назад і навіть відхилялась як така, що "з майбутнього"
- пришвидшено статистику в іноземній валюті: курси НБУ на всі дати періоду
  тепер підвантажуються паралельно (ThreadPoolExecutor), а не по одному
  запиту за раз — річний період раніше міг чекати на це хвилинами
- додано перегляд статистики в євро (третя кнопка € поруч із ₴ / $), курси
  НБУ отримуються так само, як і для долара, і кешуються окремо за валютою
- виправлено підказку на графіку "по днях": вона завжди показувала ₴,
  навіть під час перегляду статистики в доларах чи євро
- додано підтримку Excel serial dates: функція validate_date() тепер приймає як
  ISO формат (YYYY-MM-DD), так і Excel serial numbers (числа від 0 до ~60000),
  що дозволяє легко імпортувати історичні дані з Excel файлів прямо до Google Sheets