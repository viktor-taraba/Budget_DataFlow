[06.07.2026]
- Rate-limiting на /login
- іконки для категорій
- валідація введеної суми та дати на клієнті (до відправки форми)
- кнопка для перемикання на світлу / темну тему
- smoke / unit тести

[07.07.2026]
- додано pre-commit hook (для коміту мають успішно відпрацювати тести)
- додано блок "Останні записи" (топ-5 останніх значень для доходів та витрат)

[28.07.2026]
- видалення записів з блоку "Останні записи" (з підтвердженням і перевіркою,
  що рядок у таблиці не змінився з моменту відкриття сторінки)
- get_recent_entries розділено на _rows_to_entries + get_recent_entries, додано тести
- виправлено вимкнення rate-limit у тестах (limiter.enabled замість config)
- вікно "Статистика" за обраний період (7/30/90 днів або свій діапазон,
  за замовчуванням — останні 30 днів): загальні суми витрат/доходів і різниця,
  окремі щоденні графіки по витратах і доходах, таблична альтернатива графікам,
  топ-5 категорій витрат і доходів

[29.07.2026]
- редагування записів (модальне вікно з попередньо заповненими даними та перевіркою fingerprint)
- суми на сторінці статистики показуються без копійок (компактніше)
- управління категоріями: ⚙️ кнопка для додавання, видалення, редагування категорій
  (модальне вікно з табами для витрат та доходів, категорії зберігаються у categories.json)
- архівування видалених записів до таблиці DELETED з полями deleted_at та income_or_expense
- додано поле updated_at для відстеження дати редагування запису
- форматування суми з комою (515,7 замість 515.7) при записуванні в таблицю
- тести для перевірки форматування суми з комою в submit та edit маршрутах
- розбиття операції на категорії (checkbox "Розбити на категорії" в формі)
- виправлено відображення та редагування розбитих операцій:
  * видалено знак ₴ з "Останніх записів" для компактності
  * виправлено обрізування назви категорії для split expenses
  * виправлено помилку fingerprint при редаганні split (використовуємо реальну категорію першого рядка)
- додано редагування розбивки: можна змінювати категорії та суми в розбитих операціях,
  невідредаговуваний останній рядок автоматично розраховує залишок суми

[30.07.2026]
- виправлено видалення розбитих операцій: рядки шукаються за split_id, а не за
  номером рядка, тому зсув нумерації в таблиці більше не блокує видалення
  повідомленням "Запис уже змінився або був видалений"
- виправлено кнопку "Зберегти" при редагуванні розбивки: приховане поле категорії
  лишалось required, і браузер мовчки не давав відправити форму
- у модалці редагування розбивки поле суми тепер показує загальну суму операції,
  від якої рахується залишок в останньому рядку
- "+ Додати категорію" в модалці редагування справді додає рядок і не скидає
  вже введені суми; видалення рядка теж їх зберігає
- редагування розбивки коректно змінює кількість категорій: зайві рядки
  видаляються, нові дописуються (раніше в таблиці лишались рядки старої розбивки)
- відредаговані суми записуються в тому ж форматі, що й нові (515,7)
- перегляд статистики в доларах США (окремі кнопки ₴ / $ на вкладці "Статистика"),
  курси НБУ автоматично отримуються на дату операції

[31.07.2026]
- у "Статистиці" додано календарні фільтри "Поточний місяць" і "Попередній місяць"
- додано окремий режим "По місяцях" із фільтрами пів року, рік або власний
  діапазон; він показує графік і таблицю доходів, витрат, чистого результату
  та його відсотка від доходів
- додано необов'язкові підкатегорії з емоджі: вони прив'язані до категорій,
  зберігаються в окремій колонці, доступні для нових і попередніх операцій та
  керуються у вікні категорій

[01.08.2026]
- виправлено дату за замовчуванням у формі та статистиці: сервер працює в UTC,
  тепер "сьогодні" рахується за київським часом (today_kyiv()), а не часом
  сервера — раніше вночі (різниця у 2-3 год залежно від DST) дата "з'їжджала"
  на день назад і навіть відхилялась як така, що "з майбутнього"
- пришвидшено статистику в іноземній валюті: курси НБУ на всі дати періоду
  тепер підвантажуються паралельно (ThreadPoolExecutor), а не по одному
  запиту за раз — річний період раніше міг чекати на це хвилинами
- додано перегляд статистики в євро (третя кнопка € поруч із ₴ / $), курси
  НБУ отримуються так само, як і для долара, і кешуються окремо за валютою
- виправлено підказку на графіку "по днях": вона завжди показувала ₴,
  навіть під час перегляду статистики в доларах чи євро
- додано підтримку Excel serial dates: функція validate_date() тепер приймає як
  ISO формат (YYYY-MM-DD), так і Excel serial numbers (числа від 0 до ~60000),
  що дозволяє легко імпортувати історичні дані з Excel файлів прямо до Google Sheets
- виправлено валідацію сум з Google Sheets: видалено неблокуючі пробіли (U+00A0)
  з тисячних розділювачів перед парсингом, що раніше викликало тиху фільтрацію
  всіх записів із форматованими сумами
- збільшено ліміт періоду статистики з 1 року до 5 років (1825 днів), додано
  попередження користувачу при перевищенні ліміту

[02.08.2026]
- нова окрема сторінка "Курси валют" (/currency), без прив'язки до доходів/витрат:
  перемикач USD / EUR (показується один курс за раз), фільтри періоду (7/30/90 днів,
  рік, свій діапазон), лінійний графік курсу з підказками, таблична альтернатива
  графіку та підсумок періоду — курс на початок, курс в кінці, зміна у відсотках
  і напрямок (девальвація/ревальвація гривні)
- сторінка відкривається за кнопкою 💱 у шапці вікна "Статистика"
- додано get_exchange_rate_range(): курси НБУ за весь період тепер підвантажуються
  одним запитом (start/end на конкретний valcode), а не по одному запиту на кожну
  дату, як це досі робить get_exchange_rate() для конвертації статистики
- пропуски в курсах НБУ (вихідні дні без власного значення) заповнюються останнім
  відомим курсом (_fill_rate_gaps), так що графік не рветься
- у футері головної сторінки та сторінки "Курси валют" показується дата й час
  останнього коміта (на практиці — останнього змерженого PR) у київському часі;
  береться з git log поточного репозиторію, кешується на весь час роботи процесу
  і просто ховається, якщо git недоступний

[03.08.2026]
- нові категорії та підкатегорії без emoji тепер автоматично отримують
  підходящу emoji від OpenAI (додавання і перейменування): якщо назва вже
  містить emoji — LLM не викликається; якщо LLM недоступний (немає ключа,
  мережа, незрозуміла відповідь) — категорія все одно зберігається, просто
  без emoji, а користувач бачить попередження в інтерфейсі, запит ніколи не
  провалюється через це
- ключ читається з OPENAI_API_KEY (необов'язковий env), модель — з
  OPENAI_MODEL (за замовчуванням gpt-4o-mini)
- перевірка на дублікат категорії/підкатегорії тепер ігнорує ведучу emoji
  та регістр (_category_core_text), інакше "Кіно" і щойно додане "🎬 Кіно"
  вважались би різними категоріями

[07.08.2026]
- категорії (з підкатегоріями) тепер зберігаються в окремому аркуші
  "Categories" тієї ж Google-таблиці, а не лише у локальному
  categories.json — раніше кожен redeploy на Render піднімав застосунок
  у новому контейнері, і всі категорії, додані через застосунок, зникали
  без сліду
- при першому запуску після оновлення застосунок сам створює аркуш
  "Categories" і переносить туди вміст поточного categories.json
  (одноразова міграція)
- локальний categories.json лишається резервним джерелом: якщо Google
  Sheets тимчасово недоступний, категорії читаються з нього, так само
  як get_recent_entries() не ламає сторінку при збої Sheets

[08.08.2026]
- звіт електронною поштою (кількість операцій, сума доходів, сума витрат,
  чистий результат) у command-line стилі (моноширинний шрифт, темне тло
  для HTML-версії листа): за вчорашній день, надсилається через Resend
  (https://resend.com) — send-only API-ключ, на відміну від Gmail App
  Password не дає доступу до жодної поштової скриньки
- "наздоганяючий" лист за вчора надсилається не за розкладом (годинник
  ненадійний на дайні Render, що засинає), а при першому запиті кожного
  нового процесу; стан "чи вже надіслано" зберігається у
  daily_report_state.json, тому повторний запит того самого процесу не
  дублює лист, а невдала спроба автоматично повторюється на наступному запиті
- додано кнопку 📧 в шапці головної сторінки: надсилає звіт негайно за
  довільно обраний період (не лише за вчора)
- нові env-змінні (усі необов'язкові — без RESEND_API_KEY/DAILY_REPORT_EMAIL
  функція просто вимкнена): RESEND_API_KEY, RESEND_FROM_EMAIL,
  DAILY_REPORT_EMAIL

[13.08.2026]
- лист-звіт тепер містить не лише підсумок, а й суми по категоріях
  (окремо доходи/витрати, за спаданням) та — якщо обраний період не
  довший за 2 календарні місяці — повний список усіх операцій за цей
  період (дата, категорія, сума); для довших періодів список операцій
  пропускається, щоб лист не був завеликим, підсумок і категорії
  показуються завжди
- get_period_summary() тепер повертає ще й categories/transactions/
  include_transactions; render_report_text()/render_report_html()
  додають відповідні секції лише коли ці дані присутні, тож старий
  формат виклику (підсумок без деталей) і далі рендериться так само,
  як раніше
- у вікні "Надіслати звіт на пошту" додано кнопки-пресети періоду
  (як на сторінці статистики): "Останній тиждень", "Поточний місяць",
  "Попередній місяць" — заповнюють дати "Від"/"До", саму відправку
  запускає окрема кнопка "Надіслати"

[14.08.2026]
- введення операцій у доларах або євро (форма внесення й модалка
  редагування, доходи й витрати): перемикач ₴ / $ / € біля поля суми, за
  замовчуванням обрана гривня
- сума в гривні (поле amount, як і раніше) рахується за останнім доступним
  курсом НБУ — сьогодні, або вчора, якщо сьогоднішній курс НБУ ще не
  опубліковано (get_latest_exchange_rate); якщо курс недоступний узагалі —
  операція все одно зберігається (курс приймається за 1, сума у валюті й
  еквівалент у гривні збігаються), а користувачу показується попередження
  замість відмови в записі
- до COLUMN_ORDER і DELETED_COLUMN_ORDER додано три поля в кінці (той самий
  принцип зворотної сумісності, що й для subcategory): curr_amount (сума як
  її ввів користувач), exchange_rate (використаний курс) і currency
  (UAH/USD/EUR) — currency заповнюється завжди, curr_amount/exchange_rate
  лише для валютних операцій
- редагування валютної операції переобчислює курс наново на момент
  збереження (а не використовує курс, застосований при створенні запису);
  перемикання назад на гривню очищує валютні поля
- розбиті на кілька категорій операції валюту не підтримують і завжди
  залишаються в гривні — перемикач валюти в формі ховається й скидається на
  гривню, щойно активовано розбивку
- усі наявні споживачі суми (статистика, звіт на пошту, fingerprint
  видалення/редагування, підсумки по категоріях) як і раніше читають лише
  колонку amount (гривневий еквівалент) — жодних змін там не знадобилось

  [15.08.2026]
- виправлено дублювання "наздоганяючого" листа за вчора: стан "чи вже
  надіслано" тепер зберігається не у файлі daily_report_state.json (диск
  процесу на Render не персистентний — файл губився при кожному
  redeploy/сні, тому лист надсилався повторно на кожен перший запит після
  пробудження), а в новому аркуші "Emails" тієї ж Google-таблиці (один
  рядок на дату надісланого звіту), тим самим підходом, що вже
  використовується для categories.json → аркуш "Categories"

[16.08.2026]
- виправлено курс НБУ для валютних операцій (₴/$/€): раніше конвертація
  завжди бралась за курсом "сьогодні" (з фолбеком на "вчора"), незалежно від
  дати самої операції — тепер курс "прив'язаний" до дати операції: береться
  курс НБУ саме на цю дату, а якщо НБУ не публікував курс у цей день
  (вихідний/свято) — на найближчий попередній день, за який курс є
- те саме стосується редагування: якщо під час редагування валютного запису
  також змінюється дата, курс переобчислюється вже за новою датою